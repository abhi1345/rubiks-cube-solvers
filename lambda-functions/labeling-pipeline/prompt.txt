PROMPT = """
You are given an image of ONE face of a Rubik’s cube.

Return ONLY valid JSON.
Do NOT include explanation, markdown, or extra text.

Format EXACTLY:
{
  "grid": [[int,int,int],[int,int,int],[int,int,int]]
}

Color mapping:
white -> 0
yellow -> 1
red -> 2
orange -> 3
blue -> 4
green -> 5

Rules:
- Grid must be exactly 3 rows
- Each row must have exactly 3 integers
- Only numbers 0 through 5 are allowed

Example:
If the image has 3 red squares in the top row,
3 yellow squares in the bottom row,
and 3 blue squares in the middle row, return:
{
  "grid": [[2, 2, 2],[5, 5, 5],[1, 1, 1]]
}
"""